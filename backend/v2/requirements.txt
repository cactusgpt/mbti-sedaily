# AI LENS v2 runtime dependencies
#
# v2 imports v1 modules (e.g. `from clients.embedding_client import ...`) — the
# underlying deps (httpx, opensearch-py, beautifulsoup4, etc.) come from v1's
# installed environment when deploy-v2.sh bundles v1 source into the Lambda zip.
# Keep this list to what v2-native code adds on top of v1.

# =============================================================================
# AWS SDK — v1 and v2 share the same pin
# =============================================================================
boto3==1.35.0

# =============================================================================
# PostgreSQL + pgvector — v2 storage hub (pg8000 is pure Python, Lambda-friendly)
# =============================================================================
pg8000==1.31.2

# =============================================================================
# HTTP client — v2 handlers occasionally call out (same pin as v1)
# =============================================================================
httpx==0.27.0

# =============================================================================
# MCP (Model Context Protocol) — Chat Agent, added in TASK-4.1
# =============================================================================
# TODO(TASK-4.1): pin to exact version when Chat Agent work starts. Left
# commented for now to avoid guessing an unverified version in Phase 0.
# mcp==<pin-in-TASK-4.1>

# =============================================================================
# Dev / Test — excluded from Lambda package by deploy-v2.sh
# =============================================================================
pytest==8.3.0
pytest-asyncio==0.24.0
