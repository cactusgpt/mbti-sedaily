"""AI LENS v2 operational tools.

One-shot scripts (backfill, migrations, audits) that run outside Lambda.
Kept separate from ``v2/clients`` and ``v2/handlers`` so they're visibly
distinct from production code paths.
"""
