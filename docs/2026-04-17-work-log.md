# 2026-04-17 작업 내역

## 1. 사주 캐시 생성 스크립트 및 데이터 추가
- `scripts/generate_saju_cache.py`, `scripts/generate_parallel.py` 추가
- 천운(chongun) 조합별 캐시 JSON 1440개 생성 (`일간_일지_월지` 키)
- 오늘의 운세(today) 캐시 JSON 2개 생성 (`일간_일진` 키)
- 각 JSON에 NT(분석가) / NF(이야기꾼) / ST(실용주의자) / SF(공감러) 4가지 말투 포함

## 2. MBTI 성향별 운세 리라이팅 기능
- 성향 버튼(분석가/이야기꾼/실용주의자/공감러) 클릭 시 총운·오늘의 운세 텍스트가 해당 성향 말투로 변경
- `public/saju-cache/chongun.json` (9MB), `today.json` 단일 파일로 병합하여 static 서빙
- 마크다운 렌더러 구현 (`##`, `###`, `**볼드**`, `- 리스트` 지원)

## 3. /saju 페이지 제거
- 기존 백엔드 API 호출 방식의 독립 `/saju` 페이지 삭제
- `/?tab=fortune` (FortuneTab)으로 기능 통합

## 4. 운세 입력 폼 접힘 처리
- 운세 보러가기 / 저장된 만세력 불러오기 시 입력 폼(성별, 생년월일, 도시) 자동 접힘
- '다시 입력하기' 버튼으로 폼만 펼침 (결과는 유지)

## 5. MBTI 그룹 전역 동기화
- FortuneTab이 독립 state 대신 부모의 `selectedGroup` / `onMbtiChange` prop 사용
- 운세 탭 ↔ 헤더 배지 ↔ 챗봇 간 성향 모드 완전 동기화
- 챗봇 `currentGroup`이 부모 prop 변경 시 동기화되지 않던 버그 수정 (useEffect 추가)

## 6. 배포 인프라 확인
- 프론트엔드 배포: AWS S3 (`sedaily-mbti-frontend-dev`) + CloudFront (`E1QS7PY350VHF6`)
- 배포 명령: `npm run build` → `aws s3 sync out/ s3://sedaily-mbti-frontend-dev/ --delete` → CloudFront 캐시 무효화
- `.gitignore`에 `.vite/` 캐시 추가

---

## TODO
- [ ] 일간 운세 리라이팅 — today 캐시 JSON을 모든 일간×일진 조합으로 확대 생성
- [ ] 사주 결과 검증 — 만세력 계산 엔진의 정확도 검증 (실제 만세력 데이터와 대조)
